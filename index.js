import React from 'react';  // eslint-disable-line no-unused-vars
import {render} from 'react-dom'
import App from './App' // eslint-disable-line no-unused-vars

render(
    <>
        <App title="arcgis-react"/>
    </>,
    document.getElementById("app")
);
